{
  "devDependencies": {
    "parcel-bundler": "^1.12.3",
    "parcel-plugin-sw-precache": "^1.1.1",
    "sass": "1.20.1",
    "typescript": "^4.0.3"
  },
  "dependencies": {},
  "scripts": {
    "dev": "rmdir dist /s /q && parcel serve src/index.html --no-cache",
    "build": "rmdir dist /s /q && parcel build src/index.html --no-source-maps"
  },
  "sw-precache": {
    "maximumFileSizeToCacheInBytes": 4194304
  }
}
